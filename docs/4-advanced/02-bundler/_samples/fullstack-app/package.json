{
  "name": "fullstack-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node server/main.js",
    "build": "vite build client --outDir ../dist",
    "prestart": "npm run build"
  },
  "devDependencies": {
    "vite": "^4.2.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
