{
  "name": "fullstack-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node server/main.mjs",
    "build": "vite build client --outDir ../dist",
    "prestart": "npm run build"
  },
  "devDependencies": {
    "vite": "^5.0.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
