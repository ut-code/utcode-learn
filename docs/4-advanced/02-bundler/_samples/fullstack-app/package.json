{
  "name": "fullstack-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node server/main.mjs",
    "build": "vite build client --outDir ../dist",
    "prestart": "npm run build"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
